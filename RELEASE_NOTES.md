# Architecture Block Studio v0.5.1

本版本修正模块与接口的核心表达：模块名称回到顶部，输入固定左侧、输出固定右侧，并把“谁发起调用”确立为唯一接口方向。

## 核心升级

- **接口方向明确**：连接只能是 `source.output → target.input`。方法参数、返回值和错误属于同一个调用合同；主动回调、事件或命令使用另一条反向 Interface。
- **端口位置一致**：input 自动位于左侧，output 自动位于右侧；卡片端口使用 `IN / OUT` 明示角色，不再支持含义模糊的双向端口。
- **标题恢复顶部**：模块名称、类型和层级入口回到稳定顶部 Header，模块身份不再与四面端口竞争空间。
- **拖动更可控**：按住端口名称即可沿逻辑侧纵向调整位置；改变方向时 Editor 自动换侧并选择安全空位，线路实时跟随且只产生一次 Undo。
- **旧文件安全迁移**：`BlockDesignDocument 2.3` 可读取 2.0、2.1 和 2.2。旧双向端口只有在连接或父子绑定能唯一证明角色时才自动迁移；真正歧义会明确拒绝，不猜测业务语义。
- **AIO Context 示例校正**：删除返回值反向线、Canonical 所有权线、Region 成员线和假的 `build_prompt` 反向接口；重新按调用方向排布模块，避免直线反穿卡片。
- **安静画布继续保留**：画布没有可见点阵和线中标签；点击线路后在 Properties 查看完整名称与合同，手动 waypoint 仍可表达特定路径。

## 设计事实

- `direction` 是端口逻辑方向的唯一事实，`side` 只能由它派生；`offset` 保存用户决定的纵向位置。
- `BlockDesignDocument` JSON 仍是模块、端口、接口、布局和手动 waypoint 的唯一持久事实。
- 布局尺寸、缩放细节、拖动预览、语言、Dock 和最近文件都不形成第二份设计状态。

## 已知边界

- 仅发布 Windows x64，不提供网页产品、macOS、Linux、手机或平板发行。
- 自动连线保持可预测的两点直线；需要特定走向时可直接编辑 waypoint。
- 安装包尚未使用商业代码签名证书，Windows 可能显示“未知发布者”；请核对 Release 中的 SHA-256。
