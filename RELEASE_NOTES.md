# Architecture Block Studio v0.5.0

这是一次面向日常架构设计的图形编辑升级：模块改为四边对称的中心表达，端口可以直接拖动，连线默认保持简洁，并清理会干扰阅读的画布元素。

## 核心升级

- **模块身份居中**：模块名称、类型和层级入口位于卡片几何中心，不再占用顶部标题栏；四条边都完整留给端口。
- **四边端口一致**：左、右端口名称横排，上、下端口名称竖排；上下、左右采用成对对称安全区，单边端口也不会把模块名称推离中心。
- **直接拖动端口**：直接按住端口名称即可沿边移动或拖到另一条边，端口的 `side + offset` 仍是 JSON 中唯一事实。
- **更纯净的画布**：移除可见点阵背景，但保留对齐、参考线和 16px 吸附能力。
- **更清晰的连线**：自动连接收敛为端口间直接连线，不再生成不可控的复杂绕线；需要人为表达路径时仍可添加并拖动 waypoint。
- **减少线路遮挡**：移除线中标签、线桥和旧路由抓手；连接名称与契约在选中线路后的 Properties 中查看。

## 保持不变的设计事实

- `BlockDesignDocument` JSON 仍是模块、端口、接口和手动 waypoint 的唯一持久事实。
- 卡片为端口标签扩展出的尺寸、安全区和实时拖动预览都只是派生界面状态，不会制造第二份设计数据。
- Windows 客户端仍支持原生打开、保存、最近设计、自动更新、五种语言和可折叠工作区侧栏。

## 已知边界

- 仅发布 Windows x64，不提供网页产品、macOS、Linux、手机或平板发行。
- 自动连线以可读、可预测为优先，不自动表达人工希望的语义路径；需要特定走向时请直接编辑 waypoint。
- 安装包尚未使用商业代码签名证书，Windows 可能显示“未知发布者”；请核对 Release 中的 SHA-256。
